<!DOCTYPE html>
<html>
    <head>
        {{>head}}
    </head>
    <body>
        {{>navbar}}
        <div class="container">
            <div class="row">
                <div class="col-md-8 col-md-offset-2">
        {{{body}}}
                </div>
            </div>
        </div>
        {{>footer}}

        {{>modal}}
    </body>
</html>